# Public demo — skip unlock gate + auto-start guided tour
# DEMO_PUBLIC=1
# NEXT_PUBLIC_DEMO_PUBLIC=1

# Optional — override the demo unlock code (default: 333333)
# GATE_CODE=333333

# Codegen agent (NORTHPOLE builder=agent)
# BUILD_AGENT_URL=http://localhost:3000/api/build/agent-stub
# BUILD_AGENT_TOKEN=